# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NoStressParking' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for NoStressParking
pod 'Alamofire', '~> 4.4'

  target 'NoStressParkingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NoStressParkingUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
